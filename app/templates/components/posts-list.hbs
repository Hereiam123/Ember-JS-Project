{{loader-ball-pulse class=(if this.onLoading "loading" "hide")}}
<div class={{if this.onLoading "hide"}}>
  {{#each posts as |post|}}
    <article class="post">
      <button {{action "setPost" post}}>
        {{post.title}}
      </button>
    </article>
  {{/each}}
  <button class="btn-blue" {{action "getPrev"}}>
    Prev
  </button>
  <button class="btn-blue" {{action "getNext"}}>
    Next
  </button>
</div>